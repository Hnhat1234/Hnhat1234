#include <bits/stdc++.h>
#define ll long long

using namespace std;

ll x[200005];
int main(){
    ios_base::sync_with_stdio(false);cin.tie(0);
    unordered_map<ll,ll> m;
    int n; cin >> n;

    int l = 0;

    for (int i = 1;i <= n;++i){
         cin >> x[i];

         m[x[i]]
         if (m.size())
    }

}
